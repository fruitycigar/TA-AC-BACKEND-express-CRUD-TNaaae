<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title> <%= user.name%> | Details</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <link rel="stylesheet" href="">
        <style>
            
            body { font-family: "Open sans", sans-serif; line-height: 1.5; }
            nav { display: flex; justify-content: space-between; padding: 10px;} 
            .umbrella { color:crimson; font-size: 19px; font-weight: 800; text-transform: capitalize;  }
            .add-agent { background-color: magenta; font-size: 15px; font-weight: 800; color: white; padding: 7px 13px; border-radius: 4px; border: none;}
            h1 { text-align: center; color: green; font-size: 29px; padding: 20px;}
            main { padding: 0 100px; }
            h2 { color:deepskyblue; }
            p { font-weight: 550; }
            span { display: block; font-weight: 600; }
            .book-wrapper { background: grey; padding: 4px 40px; margin-bottom: 30px; border-radius: 10px; }
            a { text-decoration: none; }
            .edit-btn { display: inline-block; padding: 9px 9px; background-color: plum; color: white; text-decoration: none; text-transform: uppercase; font-weight: 600; border: none; margin-top: 20px; margin-right: 15px;}
            .edit-btn:hover { color: black; }
            .delete-btn { display: inline-block; padding: 9px 9px; background-color: red; color: white; text-decoration: none; text-transform: uppercase; font-weight: 600; border: none; margin-top: 20px;}
            .delete-btn:hover { color: black; }
        </style>
    </head>
<body>
    <nav>
        <a href="/">
            <div class="umbrella">Umbrella</div>
        </a>
        <a href="/users/new">
            <button class="add-agent">Add Agent</button>
        </a>
    </nav>
    <main>
        <h1><%= user.name%></h1>
        <h2>E-mail: <%= user.email%></h2>
        <h3>Age: <%= user.age%></h3>
        <p>Address - <%= user.address%></p>
        <span>Bio:
            <br>
            <%= user.bio%>
        </span>
        <p>Hobbies - <%= user.hobbies%></p>
        <a href="/users/<%= user.id %>/edit">
            <button class="edit-btn">Edit</button>
        </a>
        <a href="/users/<%= user.id %>/delete">
            <button class="delete-btn">Delete</button>
        </a>      
    </main>
</body>
</html>